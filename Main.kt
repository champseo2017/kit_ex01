import kotlin.math.*
import kotlin.random.*;
import java.text.NumberFormat
import java.text.DecimalFormat
fun main(args: Array<String>) {
/*
   Function แบบ Single Expression

   fun write(str: String) = println(str);
    fun inc(a: Int) = a + 1
    fun add(x: Int, y: Int) = x + y
    fun oddEven(n: Int) = if (n % 2 == 0) "Even" else "Odd"
    fun winner(p1Score: Int, p2Scroe: Int) = when {
        p1Score > p2Scroe -> "Player1"
        p1Score < p2Score -> "Player2"
        else -> "Draw"
    }
    fun numDays(month: Int, isLeapYear: Boolean) = when (month) {
        1, 3, 5, 7, 8, 10, 12 -> 31
        4, 6, 9, 11 -> 30
        2 -> if (isLeapYear) 29 else 28
        else -> 0
    }
* */

}
