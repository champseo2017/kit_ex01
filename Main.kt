import kotlin.math.*
import kotlin.random.*;
import java.text.NumberFormat
import java.text.DecimalFormat
fun main(args: Array<String>) {
/*
    การออกจาก Function
    fun showRectArea(width: Double, height: Double) {
        if (width <= 0 || height <= 0) {
            return
        } else {
            print(width * height)
        }
    }

    fun circleArea(radius: Double) : Double =
        if (radius < 0) 0.0 else 3.141 * radius * radius

        ขอบเขตตัวแปรที่อยู่ในและนอก Function
        val a: Int = 99
    fun test() {
        val b = 100
        println(a) // ok
        println(c) // ok
    }
    val c: Int = 101
    fun main() {
        test()
        println(b) // error
    }

* */

}
