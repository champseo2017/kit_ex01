fun main(args: Array<String>) {
/*
การใช้ลูปแบบ while
var n = 1
    while ((n * n) < 100) {
        n += 1
    }
    print("n = $n")

    var validCode = false
    while (validCode == false) {
        print("กรุณาใส่รหัสผ่าน >>")
        var code = readLine()
        if (code == "1234") {
            validCode = true
        }
    }
    print("คุณใส่รหัสถูกต้อง")
* */

}