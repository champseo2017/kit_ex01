/*

    พารามิเตอร์ it
    กรณีที่แลมบ์ดา มีพารามิเตอร์เพียงตัวเดียว ภายในสาวนของแลมบ์ดา เราไม่จำเป็น
    ต้องระบุพารามิเตอร์ รวมทั้งสัญลักษ์ ->
    สามารถกำหนดการกระทำต่างๆ ลงไปได้เลย
    แทนค่าพารามิเตอร์ที่รับเข้ามาด้วยคำว่า it

* */


fun main(args: Array<String>) {

  val inc: (Int) -> Int = { it + 1 }
  val oe: (Int) -> String = { if (it % 2 == 0) "Even" else "Odd" }
  val echo: (String) -> Unit = { println(it) }


}