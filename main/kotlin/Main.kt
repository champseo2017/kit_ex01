/*
    การใช้คำสั่ง if แบบ conditional expression
    -สามารถใช้คำสั่ง if-else ในแบบ expression เพื่อกำหนดค่าของ
    ตัวแปรตามเงื่อนไข

    val remain = 1
    val r = if (remain > 0) "มีสินค้า" else "สินค้าหมด"
    print(r)

    นำ if-else ไปแทรกไว้ ณ จุดที่ต้องการใช้ผลลัพธ์
    val ext = ".kt"
    print(if (ext == ".java") "Java File" else "Kotlin File")

    เขียนคำสั่ง if ในบล็อกที่ซับซ้อน
    val manuGoals = 2
    val liverGoals = 3
    val result =
        if (manuGoals > liverGoals) {
            "ManUnited ชนะ!"
        } else if (manuGoals < liverGoals) {
            "Liverpool ชนะ!"
        } else {
            "เสมอกัน"
        }

    print(result)

* */

fun main(args: Array<String>) {



}